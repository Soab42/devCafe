import Modal from "../common/modal/index";
export default function LoginModal() {
  // const { show, onClose, onSubmit, children, cname } = [];

  return (
    <Modal title={title} show={true}>
      <div>hi</div>
    </Modal>
  );
}
